/**
 * Shared localStorage key constants for NexusFlow.
 * Always import from here – never use raw strings directly.
 */

/** JWT bearer token */
export const TOKEN_KEY = "nexusflow_token";

/** Serialised user object */
export const USER_KEY = "nexusflow_user";
